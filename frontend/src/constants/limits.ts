/** Keep in sync with API `API_UPLOAD_MAX_MB` on the server (Render env). */
export const MAX_UPLOAD_MB = 5;

/** Max files per batch request (backend also enforces `BATCH_MAX_FILES`). */
export const BATCH_MAX_FILES = 2;
